export const ERROR_STOP_AND_REPORT = `If a \`vault0_task-*\` tool call returns an error (task not found, invalid transition, dependency cycle, etc.), stop immediately and report the error to the orchestrator. Do not retry with guessed parameters, silently skip the operation, or continue as if it succeeded. Tool errors indicate a state mismatch that requires human or orchestrator intervention.`
