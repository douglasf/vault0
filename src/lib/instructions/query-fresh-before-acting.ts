export const QUERY_FRESH_BEFORE_ACTING = `Before mutating a task (via \`vault0_task-update\`, \`vault0_task-move\`, or \`vault0_task-complete\`), always call \`vault0_task-view\` first to get the current state. Task status, dependencies, and metadata can change between turns — stale assumptions lead to invalid transitions or lost updates. Never skip this step, even if you viewed the task earlier in the conversation.`
